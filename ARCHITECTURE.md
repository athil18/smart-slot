# 🏗️ SmartSlot Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER BROWSER                                │
│                     http://127.0.0.1:5173/                           │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      VITE DEV SERVER                                 │
│                     Port: 5173                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  React Application (Frontend)                                 │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │  │
│  │  │   Pages     │  │ Components  │  │   Design    │           │  │
│  │  │             │  │             │  │   System    │           │  │
│  │  │ • Dashboard │  │ • Booking   │  │ • Button    │           │  │
│  │  │ • Booking   │  │   Forms     │  │ • Input     │           │  │
│  │  │ • Admin     │  │ • Confirm   │  │ • Card      │           │  │
│  │  └─────────────┘  └─────────────┘  └─────────────┘           │  │
│  │                                                                │  │
│  │  State Management: React Query (TanStack)                     │  │
│  │  Routing: React Router v7                                     │  │
│  │  Styling: TailwindCSS v4 + Framer Motion                      │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  Vite Proxy Configuration:                                           │
│  /api/* → http://127.0.0.1:3000                                      │
└────────────────────────────┬──────────────────────────────────────-─┘
                             │
                             │ Proxied API Calls
                             │ /api/v1/*
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    EXPRESS BACKEND SERVER                            │
│                          Port: 3000                                  │
│                     Host: 0.0.0.0                                    │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Middleware Stack                                             │  │
│  │  • Helmet (Security)                                          │  │
│  │  • CORS (Cross-Origin)                                        │  │
│  │  • Compression                                                │  │
│  │  • Cookie Parser                                              │  │
│  │  • Request Logger                                             │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  API Routes (/api/v1)                                         │  │
│  │                                                                │  │
│  │  ┌────────────┐   ┌────────────┐   ┌────────────┐            │  │
│  │  │   Auth     │   │  Booking   │   │   Admin    │            │  │
│  │  │            │   │            │   │            │            │  │
│  │  │ POST       │   │ GET        │   │ GET        │            │  │
│  │  │ /register  │   │ /appoint.. │   │ /users     │            │  │
│  │  │            │   │            │   │            │            │  │
│  │  │ POST       │   │ POST       │   │ POST       │            │  │
│  │  │ /login     │   │ /appoint.. │   │ /verify    │            │  │
│  │  └────────────┘   └────────────┘   └────────────┘            │  │
│  │                                                                │  │
│  │  ┌────────────┐   ┌────────────┐   ┌────────────┐            │  │
│  │  │ Resources  │   │   Slots    │   │   Tasks    │            │  │
│  │  └────────────┘   └────────────┘   └────────────┘            │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Controllers & Business Logic                                 │  │
│  │  • Input Validation (Zod)                                     │  │
│  │  • Authentication (JWT)                                       │  │
│  │  • Authorization (Role-based)                                 │  │
│  │  • Error Handling                                             │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                             │                                         │
│                             │ Prisma ORM                              │
│                             ▼                                         │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Prisma Client (Type-safe Database Access)                    │  │
│  └───────────────────────────────────────────────────────────────┘  │
└────────────────────────────┬──────────────────────────────────────-─┘
                             │
                             │ SQL Queries
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      SQLite DATABASE                                 │
│                    File: dev.db                                      │
│                                                                       │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Tables (Schema)                                              │  │
│  │                                                                │  │
│  │  User                 Task                  Resource          │  │
│  │  ├── id              ├── id                 ├── id            │  │
│  │  ├── email           ├── title              ├── name          │  │
│  │  ├── password        ├── description        ├── type          │  │
│  │  ├── name            ├── status             ├── capacity      │  │
│  │  ├── role            ├── userId             ├── status        │  │
│  │  ├── accessLevel     └── ...                └── ...           │  │
│  │  ├── verification                                             │  │
│  │  └── ...                                                      │  │
│  │                                                                │  │
│  │  Slot                 Appointment           ResourceMaint..   │  │
│  │  ├── id              ├── id                 ├── id            │  │
│  │  ├── staffId         ├── clientId           ├── resourceId    │  │
│  │  ├── resourceId      ├── slotId             ├── type          │  │
│  │  ├── startTime       ├── priority           ├── status        │  │
│  │  ├── endTime         ├── notes              └── ...           │  │
│  │  ├── status          └── ...                                  │  │
│  │  └── ...                                                      │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Data Flow: User Makes a Booking

```
1. USER
   └─> Opens http://127.0.0.1:5173/booking
       │
       ▼
2. FRONTEND (Vite)
   └─> Renders Advanced Booking Page
       ├─> Fetches available slots: GET /api/v1/slots
       │   └─> React Query manages cache
       │
       ▼
3. VITE PROXY
   └─> Proxies /api/* → http://127.0.0.1:3000
       │
       ▼
4. BACKEND (Express)
   └─> Route: GET /api/v1/slots
       ├─> Middleware: Auth check (JWT)
       ├─> Controller: slots.getAvailable()
       │   └─> Prisma: db.slot.findMany(...)
       │       │
       │       ▼
5. DATABASE (SQLite)
   └─> Query slots table
       └─> Returns available slots
           │
           ▼
6. BACKEND
   └─> Format response as JSON
       └─> Return: { success: true, data: [...] }
           │
           ▼
7. FRONTEND
   └─> React Query caches data
       └─> Renders slots in UI
           │
           ▼
8. USER
   └─> Selects a slot
       └─> Clicks "Book Now"
           │
           ▼
9. FRONTEND
   └─> POST /api/v1/appointments
       └─> Body: { slotId, clientId, notes, priority }
           │
           ▼
10. BACKEND
    └─> Validate data (Zod)
        ├─> Check slot availability
        ├─> Create appointment
        │   └─> Prisma: db.appointment.create(...)
        │       └─> Update slot status to "booked"
        │
        ▼
11. DATABASE
    └─> Insert into appointments table
        └─> Update slots table
            │
            ▼
12. BACKEND
    └─> Return success response
        └─> { success: true, data: { bookingId: "..." } }
            │
            ▼
13. FRONTEND
    └─> Show BookingConfirmation component
        └─> Display animated success message
            │
            ▼
14. USER
    └─> Sees confirmation ✅
        └─> Options: View Bookings, Cancel, Email, Calendar
```

---

## Lovable Integration Points

```
┌─────────────────────────────────────────────────────────────────────┐
│                        LOVABLE.DEV                                   │
│                 AI-Powered Frontend Builder                          │
│                                                                       │
│  Input: Natural language prompt                                      │
│  Output: React + TypeScript + TailwindCSS code                       │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             │ Copy generated code
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   YOUR SMARTSLOT PROJECT                             │
│                                                                       │
│  Replace/Enhance Components:                                         │
│                                                                       │
│  apps/frontend/src/                                                  │
│  ├── components/                                                     │
│  │   ├── booking/                                                    │
│  │   │   └── BookingConfirmation.tsx  ← PASTE HERE                  │
│  │   ├── admin/                                                      │
│  │   │   └── UserManagement.tsx       ← PASTE HERE                  │
│  │   └── ...                                                         │
│  │                                                                    │
│  ├── pages/                                                          │
│  │   ├── dashboard/                                                  │
│  │   │   ├── Dashboard.tsx            ← PASTE HERE                  │
│  │   │   └── AdvancedBooking.tsx      ← PASTE HERE                  │
│  │   └── auth/                                                       │
│  │       └── Login.tsx                ← PASTE HERE                  │
│  │                                                                    │
│  └── design-system/                                                  │
│      ├── Button.tsx                   ← ENHANCE                      │
│      ├── Input.tsx                    ← ENHANCE                      │
│      └── Card.tsx                     ← ENHANCE                      │
│                                                                       │
│  Update imports & connect to your API:                               │
│  • Change import paths to match your structure                       │
│  • Connect data fetching to /api/v1 endpoints                        │
│  • Use your existing state management (React Query)                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Technology Stack Analysis

### ✅ Compatible Technologies (Frontend & Lovable)

| Technology | Your Project | Lovable Output | Compatible? |
|------------|-------------|----------------|-------------|
| **Framework** | React 19 | React 18+ | ✅ Yes |
| **Language** | TypeScript | TypeScript | ✅ Yes |
| **Styling** | TailwindCSS v4 | TailwindCSS v3/v4 | ✅ Yes |
| **Animations** | Framer Motion | Framer Motion | ✅ Yes |
| **Icons** | Lucide React | Lucide React | ✅ Yes |
| **Forms** | React Hook Form + Zod | React Hook Form + Zod | ✅ Yes |

**Conclusion**: Lovable-generated code is **100% compatible** with your project!

---

## Deployment Architecture (Future)

```
┌────────────────┐      ┌────────────────┐      ┌────────────────┐
│   Vercel       │      │   Railway      │      │  Supabase      │
│   (Frontend)   │─────▶│   (Backend)    │─────▶│  (Postgres DB) │
│                │      │                │      │                │
│ • React build  │      │ • Express API  │      │ • Production   │
│ • Static host  │      │ • Docker       │      │   database     │
│ • CDN          │      │ • Auto-scale   │      │ • Backups      │
└────────────────┘      └────────────────┘      └────────────────┘
```

---

## Security Flow

```
1. User Login
   └─> POST /api/v1/auth/login
       └─> Backend validates credentials
           ├─> Hash password with bcrypt
           ├─> Compare with DB hash
           └─> Generate JWT tokens
               ├─> Access Token (15min)
               └─> Refresh Token (7 days)
                   │
                   ▼
2. Store Tokens
   └─> Frontend stores in memory/localStorage
       │
       ▼
3. Protected Requests
   └─> Every API call includes:
       └─> Headers: { Authorization: "Bearer <token>" }
           │
           ▼
4. Backend Middleware
   └─> Verify JWT signature
       ├─> Check expiration
       ├─> Extract user info
       └─> Attach to req.user
           │
           ▼
5. Role-Based Access
   └─> Check user.accessLevel
       ├─> 1 = Basic User
       ├─> 2 = Staff
       ├─> 3 = Advanced
       ├─> 4 = VIP
       └─> 5 = Admin
           │
           ▼
6. Execute Request
   └─> If authorized, process request
       └─> Return data
```

---

## Performance Optimization

### Frontend (Vite + React)
- **Code Splitting**: Lazy load pages
- **Tree Shaking**: Remove unused code
- **Asset Optimization**: Image compression
- **Caching**: React Query cache
- **Bundle Analysis**: Rollup visualization

### Backend (Express)
- **Compression**: gzip middleware
- **Database Indexing**: Prisma indexes
- **Connection Pooling**: Prisma pool
- **Rate Limiting**: Express rate limit
- **Caching**: Redis (future)

---

**This architecture is production-ready and scalable! 🚀**
